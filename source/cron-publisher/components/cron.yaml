apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: cron-event-publisher
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    value: "@every 15s" # valid cron schedule
  - name: route
    value: "/scheduled"
